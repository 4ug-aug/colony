# Domain docs

This is a single-context repository.

Before engineering work, read the root `CONTEXT.md` and the relevant decisions
under `docs/adr/`. Use the glossary's canonical terms in issues, tests, and
implementation notes. Surface any conflict with an ADR instead of silently
overriding it.
