# Triage labels

| Role | Local status |
| --- | --- |
| `needs-triage` | `needs-triage` |
| `needs-info` | `needs-info` |
| `ready-for-agent` | `ready-for-agent` |
| `ready-for-human` | `ready-for-human` |
| `wontfix` | `wontfix` |

Use only the statuses a workflow needs; Room threads currently use
`needs-triage` for the PRD and `ready-for-agent` for implementation slices.
